fastapi>=0.100.0,<0.112.0uvicorn[standard]>=0.20.0,<0.30.0pydantic>=2.0,<3.0httpx>=0.23.0,<0.28.0 
# For making API calls to other agents and Djangopython-dotenv>=0.20.0,<2.0.0Google Cloud specific libraries (if TTV agent itself needs to interact with GCS, e.g. for storing final videos if not handled by avatar service)google-cloud-storage>=2.7.0google-auth>=2.0.0Add any SDK for the CHOSEN Third-Party Avatar Generation serviceExample: if you choose Synthesia, you might add their Python SDK if availablesome-avatar-service-python-sdk>=1.0.0For CharacterManager and other local utilities, no extra pip installs needed if they are part of the same project.
