fastapi>=0.100.0,<0.112.0
uvicorn[standard]>=0.20.0,<0.30.0
pydantic>=2.0,<3.0
httpx>=0.23.0,<0.28.0
python-dotenv>=0.20.0,<2.0.0

# Google Cloud specific libraries
google-cloud-aiplatform>=1.40.0 # For Vertex AI (Gemini)
google-auth>=2.0.0

# Add if shared_ai_libs is a proper package and needs to be installed
# ../shared_ai_libs # This relative path might not work directly with pip install -r.
# For local dev, ensure PYTHONPATH is set. For Docker, copy shared_ai_libs in.
